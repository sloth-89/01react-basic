import React from "react";
// 이걸 입력해야 JSX가 객체 react를 받아오고 Babel이라는 웹팩을 통해 react를 유효한 Javascript로 변환할 수 있게 한다.

const MyComponent = () => {
    return <div> 컴포넌트 export, import 테스트 페이지</div>
};

export default MyComponent;